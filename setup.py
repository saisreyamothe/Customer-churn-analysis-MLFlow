from setuptools import setup, find_packages

setup(
    name="churn-prediction-mlops",
    version="0.1.0",
    description="End-to-end MLOps pipeline for churn prediction",
    packages=find_packages(),
)
